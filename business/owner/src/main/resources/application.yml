server:
  servlet:
    context-path: /api/v1
  port: 9092

spring:
  application:
    name: owner
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      username: admin
      password: admin
      name: db,general,owner
      profile: local
