server:
  servlet:
    context-path: /api/v1
  port: 9091

spring:
  application:
    name: authentication
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      username: admin
      password: admin
      name: db,general,auth
      profile: local
